<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">

<suite name="API Automation Suite" parallel="tests" thread-count="4">

    <!-- 🔑 SETUP class -->
    <test name="Suite Setup">
        <classes>
            <class name="framework.setup.TestSuiteSetup"/>
        </classes>
    </test>

    <!-- GET TESTS -->
    <test name="GET Tests">
        <packages>
            <package name="tests.json"/>
        </packages>
    </test>

    <!-- POST TESTS -->
    <test name="POST Tests">
        <packages>
            <package name="tests.post"/>
        </packages>
    </test>

    <!-- NEGATIVE TESTS -->
    <test name="Negative Tests">
        <packages>
            <package name="tests.negative"/>
        </packages>
    </test>

</suite>
